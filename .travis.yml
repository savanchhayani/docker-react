sudo: required
services:
  - docker

before_install:
  - docker build -t savanchhayani/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true savanchhayani/docker-react:latest npm run test -- --coverage
